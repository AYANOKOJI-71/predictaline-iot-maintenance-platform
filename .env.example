# PredictaLine runs entirely on local, deterministic fixture data. No secrets are required.
API_PORT=4901
WEB_PORT=5203

# Optional only when running the Vite development server against a different local API target.
# VITE_API_PROXY_TARGET=http://127.0.0.1:4901
